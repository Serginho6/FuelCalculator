<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="24dp"
    tools:context=".FuelResult">

    <ImageView
        android:src="@drawable/gas_tank"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="5dp"
        android:layout_marginHorizontal="30dp"
        android:layout_below="@id/txtResultado"
        android:layout_above="@id/boaViagem"/>

    <TextView
        android:id="@+id/txtResultado"
        android:text="Resultado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="28dp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_margin="40dp"/>

    <TextView
        android:id="@+id/boaViagem"
        android:text="Agora é só abastecer. Boa viagem!"
        android:textSize="20dp"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnVoltar"
        android:textAlignment="center"
        android:layout_margin="70dp"/>

    <Button
        android:id="@+id/btnVoltar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="VOLTAR"
        android:textSize="16dp"
        android:textStyle="bold"
        android:layout_alignParentBottom="true"
        android:layout_marginHorizontal="60dp"
        android:textColor="@color/black"
        android:backgroundTint="@color/yellow700"/>

</RelativeLayout>
